'use strict';

var _reactTestUtils = require('react-test-utils');

var _enzyme = require('enzyme');

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _viz = require('viz.js');

var _viz2 = _interopRequireDefault(_viz);

var _fixtures = require('../fixtures');

var _ = require('.');

var _2 = _interopRequireDefault(_);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/* eslint-disable import/no-unresolved */
var SwitchMock = function SwitchMock() {
  return _react2.default.createElement('div', null);
};

// jest.mock('viz.js/full.render');
jest.mock('viz.js'); // this happens automatically with automocking

var mMock = jest.fn();
_viz2.default.mockImplementation(function () {
  return {
    renderString: mMock
  };
});

it('transforms types to edges', async function () {
  var promise = Promise;
  mMock.mockReturnValue(promise);
  var wrapper = (0, _enzyme.mount)(_react2.default.createElement(_2.default, { types: _fixtures.types, Switch: SwitchMock }));
  (0, _reactTestUtils.act)((await promise.resolve('<svg/>')));
  expect(wrapper).toMatchSnapshot();
});